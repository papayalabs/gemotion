<div class="flex flex-col items-center mb-45">
  <h2 class="text-left">Ajoutez votre partie vidéo à la dédicace finale</h2>
  <%= render "shared/profile_tabs", page_count: 1 %>
</div>

<div class='spaces-on-sides'>
  <div>
    <h1 class="video-h1">Sélectionnez les chapitres pour le film</h1>
    <p class="video-desc">Ipsum dolor sit amet consectetur. Dictum eu morbi ac elit enim at neque urna. Eleifend condimentum ipsum a lectus tortor est arcu. Feugiat lectus auctor cursus netus eu sit in pharetra. Convallis eget odio molestie urna phasellus etiam ac euismod justo. Ipsum tristique purus risus at ultrices non. Eget lectus diam eget feugiat nulla imperdiet ornare libero.</p>
    <div class="">
      <%= form_with url: creator_chapters_post_path(video_id: @video.id), method: :post do |f| %>
        <div class="frame">
          <h3 class="video-h3">Choisissez les chapitres qui figureront dans le film ( 12 chapitres max )</h3>
          <p>L'introduction de votre vidéo dépendra de l'option que vous avez sélectionné</p>
          <div class="video-chapters">
            <% @chapterstype.each do |res| %>

              <% checkbox_test_id = "chapter_checkbox_#{res[:ct].id}" %>
              <% input_test_id = "chapter_input_#{res[:ct].id}" %>
              <% slide_color_test_id = "chapter_slide_color_#{res[:ct].id}" %>
              <div class="video-chapter">
                <div class="video-chapter-wrapper">
                  <%= f.check_box "chapters[#{res[:ct].id}][select]", {checked: res[:select], 'data-testid': checkbox_test_id }, true, false %>
                  <div class="video-chapter-thumb-wrapper">
                    <div>
                      <div data-controller="dialog" data-action="click->dialog#clickOutside">
                        <%= image_tag "icons/akar-icons_edit.svg", alt: "Edit Icon", class: "edit-prof-video-icon", data: { action: "click->dialog#open" } %>
                        <dialog data-dialog-target="modal" class="backdrop:bg-slate-400 backdrop:opacity-80 w-full lg:w-2/3 popup-chap-modal">
                          <h3 class="video-h3 mt-2 mb-[32px]">Personnalisez votre film! </h3>
                          <div class="w-full flex-im flex-row justify-center items-start">
                            <div class="chapter-popup-block">
                              <div class="video-chapter-thumb-wrapper">
                                <div>
                                  <% if res[:ct].image.attached? %>
                                    <%= image_tag res[:ct].image, class: "video-chapter-thumb" %>
                                  <% else %>
                                    <p>Pas d'image</p>
                                  <% end %>
                                  <span><%= res[:ct].name %></span>
                                </div>
                                <p class="mt-2 mb-2">“Description des chapitres lorem ipsum dolor sit amet consectetur. Dictum eu morbi ac elit enim at neque urna. Eleifend condimentum ipsum a lectus tortor est arcu”</p>
                                <div class="font-bold">Choisissez la couleur du diapositive</div>
                                <div class="color-picker flex-im flex-row justify-start items-center mt-2">
                                  <% colors = { black: 'black', grey: 'grey', white: 'white' } %>
                                  <% default_color = colors.keys.first.to_s %> <!-- Set the default color to the first in the list -->
                                  <% colors.each do |color_name, color_value| %>
                                    <%= f.radio_button "chapters[#{res[:ct].id}][slide_color]", color_value,
                                                      id: "#{color_value}_#{res[:ct].id}",
                                                      checked: (res[:slide_color] == color_value || (res[:slide_color].nil? && color_value == default_color)) %>
                                    <%= f.label "#{color_value}_#{res[:ct].id}", "&nbsp;".html_safe, class: "color-circle #{color_value}" %>
                                  <% end %>
                                </div>
                                <div class="font-bold mt-2">Text</div>
                                <div class="text-options">
                                  <!-- Select box for Text Family -->
                                  <div class="form-group">
                                    <%= f.select "chapters[#{res[:ct].id}][text_family]", options_for_select(['Poppins','Arial', 'Helvetica', 'Times New Roman', 'Verdana', 'Courier New'], res[:text_family]), {}, class: 'form-select w-full chapter-popup-select' %>
                                  </div>

                                  <!-- Select box for Text Style -->
                                  <div class="form-group">
                                    <%= f.select "chapters[#{res[:ct].id}][text_style]", options_for_select(['Normal', 'Bold', 'Italic', 'Bold Italic'], res[:text_style]), {}, class: 'form-select w-full chapter-popup-select' %>
                                  </div>
                                  <!-- Select box for Text Size -->
                                  <div class="form-group">
                                    <%= f.select "chapters[#{res[:ct].id}][text_size]", options_for_select([12, 14, 16, 18, 20, 24, 28, 32, 36, 40, 44, 48, 52, 56], res[:text_size]), {}, class: 'form-select w-full chapter-popup-select' %>
                                  </div>
                                </div>
                                <div class="prev-your-text flex-im justify-start items-center">Aperçu de votre texte</div>
                                <%= button_tag type: 'button', formmethod: 'dialog', class: 'cta w-100i', data: { action: 'click->dialog#close' } do %>
                                  Appliquer les modifications
                                  <%= image_tag 'icons/mdi_magic.png', alt: 'Download Icon', class: 'inline-block', style: 'height: 16px; width: 16px;' %>
                                <% end %>
                              </div>
                            </div>
                          </div>

                        </dialog>
                      </div>
                      <% if res[:ct].image.attached? %>
                        <%= image_tag res[:ct].image, class: "video-chapter-thumb" %>
                      <% else %>
                        <p>Pas d'image</p>
                      <% end %>
                      <span><%= res[:ct].name %></span>

                    </div>
                    <%= f.text_field "chapters[#{res[:ct].id}][text]", value: res[:text], placeholder: "Quel texte doit contenir ce chapitre ...", data: {testid: input_test_id} %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
        <div class="flex justify-center items-center mt-6">
          <%= f.submit "Sauvegarder les modification", class:"cta" %>
        </div>
      <% end %>
    </div>
  </div>
