<div>

  <%if @video.colab?%>
    <h1 class="video-h1">Invitez vos proches à participer  à votre projet</h1>
  <%else%>
    <h1 class="video-h1">Invitez des amis à collaborer sur  votre projet !</h1>
  <%end%>

  <div class="flex flex-row justify-center w-full">
    <%= image_tag 'left-no-star.png', class: 'image-sides left-image' %>
    <div class="content w-full flex flex-col justify-center items-center">

      <div class="">

        <div class="frame">

          <h4 class="video-h4">Envoyez une invitation via un lien  whatsapp, facebook, instagram ou en envoyant un email</h4>

          <%= form_with url: invite_collaborators_post_path(video_id: @video.id), method: :post do |f| %>
            <div class="share-wrapper">
              <%= f.email_field :email, placeholder: "Email", class:"share-email" %>
              <%= f.submit "Inviter", class:"share-submit" %>
            </div>
          <% end %>

          <p class="p-text-16">Ou partagez le lien</p>

          <div class="share-clipboard" data-controller="clipboard">
            <div class="share-clipboard-wrapper">
              <%= image_tag "icons/tabler_link.png" %>
              <span><%= join_url(@video.token) %></span>
            </div>
            <span data-action="click->clipboard#copy" data-clipboard-value-param="<%= join_url(@video.token) %>" class="share-clipboard-btn">Copier</span>
          </div>

          <div class="flex flex-row justify-center items-center">
            <%= image_tag "icons/whats-share.png", class: 'share-icon-whats'%>
            <%= image_tag "icons/facebook-share.png", class: 'share-icon-facebook' %>
          </div>
        </div>

        <div class="flex flex-row justify-center items-center mt-35">
          <%= link_to "Retour à la page précédente", participants_progress_path(video_id: @video.id), class: "black-link", data: {turbo: false} %>
        </div>
      </div>
    </div>
    <%= image_tag 'right-with-star.png', class: 'image-sides right-image' %>
  </div>
</div>